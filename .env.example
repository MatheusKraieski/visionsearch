# Nunca commitar o .env.local — apenas este .env.example vai para o git.
# Copie para .env.local e preencha os valores.

# Chave da API Gemini — configure no painel do Vercel em Settings > Environment Variables
# Obter em: https://aistudio.google.com/apikey
GEMINI_API_KEY="SUA_CHAVE_AQUI"
